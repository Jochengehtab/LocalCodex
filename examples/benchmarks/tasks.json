[
  {"id":"plan-1","category":"plan","prompt":"Create a plan to implement clamp in bench_project.py. Cover inverted bounds and boundaries. Do not modify files.","acceptance":"No file changes; specifies inclusive bounds, ValueError and boundary tests."},
  {"id":"plan-2","category":"plan","prompt":"Review median in bench_project.py. Explain three correctness issues without modifying files.","acceptance":"Identifies even-length result, mutation and empty-input handling; no file changes."},
  {"id":"plan-3","category":"plan","prompt":"Create a plan to separate parsing, calculations and CLI concerns in this example without introducing services or external dependencies. Do not modify files.","acceptance":"Names responsibilities, a staged migration and regression tests; no file changes."},
  {"id":"build-1","category":"build","prompt":"Implement only clamp in bench_project.py according to its docstring. Run python -m unittest checks.BuildChecks.test_clamp.","acceptance":"Named test passes; checks.py unchanged; unrelated functions unchanged."},
  {"id":"build-2","category":"build","prompt":"Implement only parse_ids in bench_project.py according to its docstring. Run python -m unittest checks.BuildChecks.test_parse_ids.","acceptance":"Named test passes; checks.py unchanged; unrelated functions unchanged."},
  {"id":"build-3","category":"build","prompt":"Implement only ordered_unique in bench_project.py according to its docstring. Run python -m unittest checks.BuildChecks.test_ordered_unique.","acceptance":"Named test passes; checks.py unchanged; unrelated functions unchanged."},
  {"id":"vision-1","category":"vision","prompt":"Read the attached diagram. Name the three destinations of the router. Do not read source files.","acceptance":"Plan, Build and Vision; actual image attachment used."},
  {"id":"vision-2","category":"vision","prompt":"Read the attached diagram. Which component is directly connected to Codex CLI, and how many outgoing branches does that component have? Do not read source files.","acceptance":"Router; three outgoing branches; actual image attachment used."},
  {"id":"vision-3","category":"vision","prompt":"Read the attached diagram. Describe the path to the bottom-right box and name that box. Do not read source files.","acceptance":"Codex CLI → Router → Vision; actual image attachment used."},
  {"id":"recovery-1","category":"recovery","prompt":"Run python -m unittest checks.RecoveryChecks.test_median. Inspect the failure, fix only median in bench_project.py and rerun the test.","acceptance":"Real failing tool output followed by passing named test; checks.py unchanged."},
  {"id":"recovery-2","category":"recovery","prompt":"Run python -m unittest checks.RecoveryChecks.test_percentage. Inspect the failure, fix only percentage in bench_project.py and rerun the test.","acceptance":"Real failing tool output followed by passing named test; checks.py unchanged."},
  {"id":"recovery-3","category":"recovery","prompt":"Run python -m unittest checks.RecoveryChecks.test_chunks. Inspect the failure, fix only chunks in bench_project.py and rerun the test.","acceptance":"Real failing tool output followed by passing named test; checks.py unchanged."}
]
